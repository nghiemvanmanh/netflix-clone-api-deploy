export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum typeAudioMovie {
  SUB = 'Subtitle',
  AUDIO = 'Audio',
}

export enum typeNotification {
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info',
  WARNING = 'warning',
}
